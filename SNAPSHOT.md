# legacy-v1/add-calendar — v1 snapshot

Captured from `Kromatic-Innovation/voltaire` for the v2 rewrite (epic #46, snapshot issue #49).

| Field | Value |
|---|---|
| Voltaire commit | `0b13cd0e0fd502dbc04c70f07e500e4b7b8e4c94` |
| Skill first authored | 2026-04-01 |
| First author | Tristan Kromer |
| Snapshot date | 2026-05-11 |
| Branch base | `a20a3a3` (pre-v2 fork main tip) |

## What's here

The contents of `.claude/skills/add-calendar/` as they existed in voltaire at the time of snapshot. This is the **SKILL.md and any colocated resources only** — the implementation files referenced by the SKILL.md (under `src/`, `container/agent-runner/src/`, `scripts/`, etc.) are NOT copied. To retrieve them, check voltaire at `0b13cd0e0fd502dbc04c70f07e500e4b7b8e4c94`.

## What it is NOT

This branch is **not buildable**. It is a v1-architecture snapshot for reference during the v2 rewrite. Do not merge it anywhere; do not install it. Its purpose is to give the v2 rewrite a stable diff target inside the fork's branch namespace.

The v2 rewrite lives on a separate `feat/<name>-v2` branch and ultimately splits into `<name>-upstream` + `skill/<name>` per the epic's branching model.
