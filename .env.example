SLACK_BOT_TOKEN=
SLACK_APP_TOKEN=

# Memory: who can write global memories (readable by all groups)
# "any" = all groups can write global memory (default)
# "main_only" = only the main group can write global memory
MEMORY_GLOBAL_WRITE_POLICY=any
